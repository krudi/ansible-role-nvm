---
# Variable to get current home path
home: "{{ lookup('env', 'HOME') }}"

# Variable to get current user
user: "{{ lookup('env', 'USER') }}"

# Needed dependencies to install Node Version Manager
setup_dependencies_list:
  - build-essential
  - libssl-dev
  - curl
